/*
Дополните (замените нижние подчеркивания) код, чтобы определить функцию, которая принимает две строки в качестве аргументов 
и возвращает результат их объединения (конкатенации).

Sample Input:
concat enation

Sample Output:
concatenation
*/

//For this tasks, we need to write only function, package "fmt" and func main () already declared.

func concat(a, b string) string {
    return a + b
}
